@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Threads)

include("${CMAKE_CURRENT_LIST_DIR}/logfuncTargets.cmake")
if(NOT TARGET logfunc::logfunc)
    add_library(logfunc::logfunc ALIAS logfunc::header_only)
endif()

check_required_components(logfunc)
